version: "3.3"

services:
  web:
    build: .
    container_name: fastapi_mini_app
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
